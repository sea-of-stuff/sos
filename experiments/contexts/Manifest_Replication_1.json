{
  "context": {
    "name": "ManifestReplication",
    "domain": {
      "type": "LOCAL",
      "nodes": []
    },
    "codomain": {
      "type": "LOCAL",
      "nodes": []
    }
  },
  "predicate": {
    "type": "Predicate",
    "predicate": "CommonPredicates.AcceptAll();",
    "dependencies": []
  },
  "max_age": 0,
  "policies": [
    {
      "type" : "Policy",
      "apply" : "CommonPolicies.replicateManifest(codomain, utilities, manifest, factor);",
      "satisfied" : "return CommonPolicies.manifestIsReplicated(codomain, utilities, manifest, factor);",
      "dependencies" : [],
      "fields" : [
        {
          "type" : "int",
          "name" : "factor",
          "value" : "1"
        }
      ]
    }
  ]
}
